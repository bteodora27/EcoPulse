<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- PERMISIUNI INTERNET -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- PERMISIUNI LOCAȚIE -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- CAMERA -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- ANDROID 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- ANDROID 12 și mai jos -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- EVITĂM WARNING LA CAMERA FIZICĂ -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />


    <application
        android:allowBackup="true"
        tools:replace="allowBackup"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcoPulse"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- FILE PROVIDER (UNIC, CORECT) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- SPLASH -->
        <activity
            android:name=".splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LOGIN -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="false" />

        <!-- REGISTER (dacă există) -->
        <activity
            android:name=".auth.SignUpActivity"
            android:exported="false" />

        <!-- MAIN -->
        <activity
            android:name=".main.MainActivity"
            android:exported="false" />

        <!-- MAP CLEANUP SESSION -->
        <activity
            android:name=".cleanup_session.ContinueCleanupActivity"
            android:exported="false" />

        <!-- UPLOAD ACTIVITY -->
        <activity
            android:name=".profile.UploadActivity"
            android:exported="false" />

        <activity
            android:name=".events.EventDetailsActivity"
            android:exported="false" />

        <!-- AUTUMN PROMO SCREEN -->
        <activity
            android:name=".events.AutumnPromoActivity"
            android:exported="false" />

        <!-- MAPS API KEY -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCQVvIMnb30QFZum4y7iaPC6MJpEOkkmNk"/>


        <!-- GOOGLE MAPS KEY -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCQVvIMnb30QFZum4y7iaPC6MJpEOkkmNk" />

    </application>

</manifest>
